# Core runtime dependencies for the end-to-end pipeline
pydantic>=2.11,<2.13
lancedb>=0.14
pyarrow>=14.0
numpy>=1.24
python-pptx>=0.6.21
pdf2image>=1.17.0
Pillow>=10.0.0

# UI / developer workflow
streamlit>=1.28.0
watchdog>=4.0.0

# Windows-only optional backend for higher-fidelity PPTX rendering via PowerPoint COM
pywin32>=311; platform_system == "Windows"

# Optional model inference stack
torch>=2.1.0
transformers>=4.45.0
diffusers>=0.30.0

# OCR support (Python wrappers only; Windows still needs external Tesseract binary for pytesseract)
pytesseract>=0.3.10
easyocr>=1.7.0

# Test / config helpers
pytest>=8.0.0
pydantic-settings>=2.2.1

# Notes:
# - bitsandbytes is not included in the default Windows install list because it is not reliably available on Windows.
# - tantivy / FTS support is not currently wired into this codebase; add it only if you enable that retrieval path.
# - Poppler and LibreOffice are system dependencies and must be installed separately on Windows.
# - If Microsoft PowerPoint is installed, set `PPT_RENDER_BACKEND=powerpoint` for the best PPTX-to-image fidelity.